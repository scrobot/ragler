---
sidebar_position: 1
title: Version 1.0.0
---

# RAGler v1.0.0

**Release Date:** February 8, 2026
**Type:** Major Release (Initial Public Release)

## Overview

RAGler v1.0.0 is the first public release of the RAGler Knowledge Management System for RAG (Retrieval-Augmented Generation). This release provides a complete, production-ready platform for Human-in-the-Loop knowledge curation.

## Highlights

✨ **Human-in-the-Loop Workflow** — Edit and validate chunks before publishing
🔄 **Atomic Replacement** — Clean content updates with no duplicates
📊 **Three Data Sources** — Confluence, Web URLs, and Manual input
👥 **Role-Based Access** — Simple Mode for L2 Support, Advanced Mode for Developers/ML
🎯 **Collection Management** — Organize knowledge by context and audience
🤖 **LLM-Powered Chunking** — GPT-4o for intelligent semantic chunking

## Core Features

### Data Ingestion

- **Confluence Integration** — Import pages via URL with API authentication
- **Web Scraping** — Extract content from public URLs with HTML→Markdown conversion
- **Manual Input** — Direct text entry for custom content
- **Automatic Chunking** — LLM-based semantic splitting using GPT-4o
- **Source Tracking** — MD5 source_id for atomic replacement

### Draft Sessions

- **Redis-Based Sessions** — Ephemeral editing sandbox with 24-hour TTL
- **Session States** — DRAFT → LOCKED → PUBLISHED lifecycle
- **Chunk Editing** — Modify content before publishing
- **Preview Mode** — Validate chunks with locking mechanism
- **Session Management** — List, view, and discard draft sessions

### Chunk Operations (Advanced Mode)

- **Split Chunks** — Divide chunks at semantic boundaries
- **Merge Chunks** — Combine related chunks into one
- **Reorder Chunks** — Adjust chunk sequence
- **LLM Assistant** — Scenarios for simplifying, clarifying, and enriching content

### Collections

- **Dynamic Collections** — Create collections via API (no hardcoded domains)
- **Collection Registry** — Stored in Qdrant `sys_registry` collection
- **Purpose-Driven Organization** — Collections describe context and audience
- **Collection Metadata** — Name, description, purpose, creator, timestamps

### Publishing

- **Atomic Replacement** — Delete-Insert strategy prevents duplicates
- **Embedding Generation** — OpenAI text-embedding-3-small (1536 dimensions)
- **Mandatory Collection Selection** — Cannot publish without target collection
- **Batch Processing** — Configurable batch size for embeddings (default: 100)

### Role-Based Access Control

**Simple Mode (L2 Support):**
- View and edit chunk content
- Use LLM assistant scenarios
- Publish to existing collections
- Cannot split/merge chunks or create collections

**Advanced Mode (Developers, ML Specialists):**
- All Simple Mode capabilities
- Split and merge chunks
- Create and delete collections
- Bulk operations (future)

### API

- **REST API** — NestJS backend with OpenAPI documentation
- **Authentication Headers** — `X-User-ID` and `X-User-Role` (trusted headers for MVP)
- **Rate Limiting** — Configurable throttling (default: 100 req/min)
- **Health Checks** — `/health/live` and `/health/ready` endpoints
- **Swagger UI** — Interactive API documentation at `/api/docs`

### Infrastructure

- **Backend** — Node.js 20+ with NestJS framework
- **Frontend** — Next.js 18 with Metronic UI (optional)
- **Redis** — Session storage (ephemeral)
- **Qdrant** — Vector database for published chunks
- **OpenAI** — GPT-4o (chunking), GPT-4o-mini (enrichment), text-embedding-3-small (vectors)
- **Docker Compose** — Infrastructure setup with Redis + Qdrant

### Observability

- **Structured Logging** — JSON logs with correlation IDs (request_id, session_id, user_id, source_id)
- **Key Events** — ingest_start, chunking_success, publish_start, publish_success, etc.
- **Error Handling** — Consistent error response format
- **Metrics-Ready** — Prometheus-compatible log format (metrics integration future)

## Architecture Decisions (ADRs)

This release implements the following architectural decisions:

1. **ADR-001: Vector-Only Storage** — No SQL database; all data in Qdrant or Redis
2. **ADR-002: Atomic Replacement** — Delete-Insert strategy for clean updates
3. **ADR-003: Two-LLM Pattern** — GPT-4o for chunking, GPT-4o-mini for enrichment
4. **ADR-004: Dynamic Collections** — Collections managed via API, stored in Qdrant

See [Architecture Decision Records](/docs/architecture/adr) for details.

## API Endpoints

### Collections

- `GET /api/collections` — List all collections
- `GET /api/collections/:id` — Get collection by ID
- `POST /api/collections` — Create collection (DEV/ML only)
- `DELETE /api/collections/:id` — Delete collection (DEV/ML only)

### Ingestion

- `POST /api/ingest` — Start ingestion session (all source types)

### Sessions

- `GET /api/sessions` — List user's sessions
- `GET /api/sessions/:id` — Get session details
- `PATCH /api/sessions/:id/chunks/:chunkId` — Edit chunk
- `POST /api/sessions/:id/chunks/:chunkId/split` — Split chunk (DEV/ML only)
- `POST /api/sessions/:id/chunks/merge` — Merge chunks (DEV/ML only)
- `POST /api/sessions/:id/preview` — Lock and validate session
- `POST /api/sessions/:id/publish` — Publish to collection
- `DELETE /api/sessions/:id` — Discard session

### Health

- `GET /health/live` — Liveness probe
- `GET /health/ready` — Readiness probe (checks Redis + Qdrant)

## Configuration

### Environment Variables

**Required:**
- `OPENAI_API_KEY` — OpenAI API key for LLM operations

**Infrastructure:**
- `REDIS_HOST` — Redis hostname (default: localhost)
- `REDIS_PORT` — Redis port (default: 6379)
- `QDRANT_URL` — Qdrant connection URL (default: http://localhost:6333)

**Optional:**
- `CONFLUENCE_BASE_URL` — Atlassian Confluence instance URL
- `CONFLUENCE_USER_EMAIL` — Confluence user email
- `CONFLUENCE_API_TOKEN` — Confluence API token
- `THROTTLE_TTL` — Rate limit window in ms (default: 60000)
- `THROTTLE_LIMIT` — Max requests per window (default: 100)

See [Configuration Guide](/docs/getting-started/configuration) for all options.

## Breaking Changes

**Not applicable** — This is the initial release.

## Known Limitations (MVP Scope)

The following features are **not included** in v1.0:

- Real-time Confluence synchronization
- Bulk document crawling
- Retrieval quality metrics
- A/B testing of RAG answers
- Automatic chunk quality scoring
- Chunk versioning
- Bulk operations in Simple Mode
- Collection migration (moving chunks between collections)
- Custom chunking algorithms
- Partial updates (always full atomic replacement)

These features may be added in future releases based on user feedback.

## Installation

### Quick Start

```bash
# Clone repository
git clone https://github.com/ragler-oss/ragler.git
cd ragler/backend

# Install dependencies
pnpm install

# Configure environment
cp .env.example .env
# Edit .env and set OPENAI_API_KEY

# Start infrastructure
docker compose up -d redis qdrant

# Start backend
pnpm start:dev
```

See [Installation Guide](/docs/getting-started/installation) for detailed setup.

## Upgrade Guide

**Not applicable** — This is the initial release.

Future upgrades will include migration guides here.

## Contributors

RAGler v1.0.0 was developed by:

- **Core Team** — System architecture, backend, frontend, documentation
- **Community** — Testing, feedback, issue reporting

Special thanks to all contributors who made this release possible!

## What's Next?

Planned for v1.1:

- Search/retrieval endpoint (currently chunks are published but not queryable via API)
- Enhanced LLM assistant scenarios
- Improved chunking quality metrics
- Bulk session operations
- MCP Server implementation (Model Context Protocol)

See [GitHub Issues](https://github.com/ragler-oss/ragler/issues) for the full roadmap.

## Feedback & Support

- **Issues:** [GitHub Issues](https://github.com/ragler-oss/ragler/issues)
- **Discussions:** [GitHub Discussions](https://github.com/ragler-oss/ragler/discussions)
- **Documentation:** [docs.ragler.ai](https://ragler.ai/docs)

## License

RAGler is released under the [MIT License](https://github.com/ragler-oss/ragler/blob/main/LICENSE).

---

**Happy knowledge curation! 🎉**

Thank you for choosing RAGler for your RAG knowledge management needs.
