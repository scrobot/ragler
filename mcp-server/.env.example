# KMS-RAG Backend API Configuration
KMS_API_URL=http://localhost:3000

# MCP Server Identity (sent as headers to backend)
MCP_USER_ID=mcp-server
MCP_USER_ROLE=DEV

# Future: API Key (not currently used by backend)
KMS_API_KEY=
